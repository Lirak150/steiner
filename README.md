# Деревья Штейнера на графах ограниченной древесной ширины

# Как пользоваться?

Запустить скрипт single_experiment.py, указав в CLI параметре путь к файлу в формате SteinLib.

```python3 single_experiment.py <путь к файлу>```